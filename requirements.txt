# Python packages for PRISM (PR Impact dashboard).
# Install with:  .venv\Scripts\python.exe -m pip install -r requirements.txt

flask==3.1.3              # the web server (serves the dashboard + API)
gunicorn==23.0.0          # production WSGI server (used on Render)
python-calamine==0.8.2   # fast Excel reader (the heavy report read)
openpyxl==3.1.5           # write the analysis .xlsx workbook
python-dotenv==1.2.3      # (optional) load config from .env

# Not required for the current template-based build (kept for reference):
# anthropic==0.122.0      # only if an LLM layer is added later
